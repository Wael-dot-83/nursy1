# 🔐 معلومات تسجيل الدخول - Nursery Management System

## ✅ **تم الانتقال بنجاح إلى MySQL 8.0**

### **بيانات قاعدة البيانات**
- **نوع القاعدة**: MySQL 8.0
- **اسم المستخدم**: `root`
- **كلمة المرور**: `83@Wael1`
- **اسم قاعدة البيانات**: `nursery_db`
- **المنفذ**: `3308` (على الجهاز المحلي) → `3306` (داخل الحاوية)

---

## 🌐 **الوصول إلى التطبيق**

### **الواجهة الأمامية**
- **الرابط**: http://localhost:4173
- **اللغة**: عربي / English (يمكن التبديل)

### **واجهة برمجة التطبيقات (API)**
- **الرابط**: http://localhost:8000
- **التوثيق**: http://localhost:8000/docs (Swagger UI)
- **فحص الصحة**: http://localhost:8000/health

---

## 👤 **بيانات تسجيل الدخول**

### **المسؤول الرئيسي (Admin)**
```
البريد الإلكتروني: admin@example.com
كلمة المرور: Admin123!
```

**ملاحظة مهمة**: 
- ❌ **لا تستخدم** `admin@nursery.local` (نطاق محجوز وغير مقبول)
- ✅ **استخدم** `admin@example.com`

---

## 🐛 **المشكلة التي تم حلها**

### **الأعراض**
- خطأ: "فشل في الاتصال بالخادم"
- خطأ: "عدد كبير جداً من المحاولات"
- حالة HTTP 422 (Unprocessable Entity)
- حالة HTTP 403 (Forbidden)

### **الأسباب الجذرية**

1. **البريد الإلكتروني غير صالح**: `admin@nursery.local`
   - نطاق `.local` محجوز ولا تقبله مكتبة Pydantic
   - **الحل**: تغيير إلى `admin@example.com`

2. **عدم تطابق الأدوار (Role Mismatch)**:
   - قيمة `RoleEnum.ADMIN` في الكود: `"admin"` (أحرف صغيرة)
   - قيمة `role` في قاعدة البيانات: `"ADMIN"` (أحرف كبيرة)
   - الواجهة الأمامية ترسل: `"ADMIN"` (أحرف كبيرة)
   - **الحل**: تحديث قاعدة البيانات لاستخدام `"admin"` بأحرف صغيرة

### **الإصلاحات المطبقة**

```sql
-- 1. تحديث البريد الإلكتروني
UPDATE users 
SET email = 'admin@example.com', 
    email_normalized = 'admin@example.com' 
WHERE id = 1;

-- 2. تحديث الدور
UPDATE users 
SET role = 'admin' 
WHERE id = 1;
```

---

## 🎯 **الخطوات التالية**

### **1. اختبار تسجيل الدخول**
1. افتح http://localhost:4173
2. أدخل البريد: `admin@example.com`
3. أدخل كلمة المرور: `Admin123!`
4. اضغط "تسجيل الدخول"

### **2. إنشاء روضة جديدة**
- الذهاب إلى: إدارة الروضات
- اضغ على "إضافة روضة جديدة"
- اختر المحافظة من القائمة (12 محافظة متاحة)
- أكمل بيانات الروضة

### **3. إنشاء فروع ومديرين**
- بعد إنشاء الروضة، يمكنك إضافة فروع
- تعيين مديرين لكل فرع

---

## 📊 **حالة الخدمات**

جميع الخدمات تعمل بشكل صحيح:

```
✅ MySQL 8.0:     Healthy (port 3308:3306)
✅ Backend API:   Healthy (port 8000)
✅ Frontend:      Healthy (port 4173)
✅ Redis Cache:   Healthy (port 6379)
```

---

## 🔧 **أوامر مفيدة**

### **فحص حالة الخدمات**
```powershell
docker compose ps
```

### **عرض سجلات الخادم الخلفي**
```powershell
docker logs nursy_backend --tail 50
```

### **الاتصال بقاعدة البيانات MySQL**
```powershell
docker compose exec db mysql -uroot -p'83@Wael1' nursery_db
```

### **إعادة تشغيل جميع الخدمات**
```powershell
docker compose restart
```

### **إيقاف جميع الخدمات**
```powershell
docker compose down
```

### **بدء جميع الخدمات**
```powershell
docker compose up -d
```

---

## 📝 **ملاحظات إضافية**

1. **المحافظات المتاحة** (12 محافظة أردنية):
   - عمان، إربد، الزرقاء، البلقاء، مادبا، جرش
   - عجلون، الكرك، الطفيلة، معان، المفرق، العقبة

2. **الترميز**: UTF-8 مع دعم كامل للعربية

3. **الأدوار المتاحة**:
   - `admin` - المسؤول الرئيسي
   - `manager` - مدير الفرع
   - `director` - مدير الروضة
   - `supervisor` - المشرف
   - `parent` - ولي الأمر

---

## ✅ **تم الانتهاء من الترحيل**

**التاريخ**: 5 نوفمبر 2025
**الحالة**: ✅ نجح الانتقال من PostgreSQL إلى MySQL 8.0
**الاختبار**: ✅ جميع الخدمات تعمل بشكل صحيح
**البيانات**: ✅ تم تحميل 12 محافظة ومستخدم admin

---

**تم إنشاء هذا الملف بواسطة**: GitHub Copilot
**آخر تحديث**: 5 نوفمبر 2025، 21:12
